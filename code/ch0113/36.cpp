/*
 * @Author: allen
 * @Date: 2023-08-11
 *
 * 36:二进制分类
 *
 * 总时间限制: 1000ms 内存限制: 65536kB
 *
 * 描述
 *     <p class="MsoNormal">若将一个正整数化为二进制数，在此二进制数中，我们将数字<span>1</span>的个数多于数字<span>0</span>的个数的这类二进制数称为<span>A</span>类数，否则就称其为<span>B</span>类数。<p class="MsoNormal">例如：<p class="MsoNormal"><span>(13)</span><sub><span>10 </span></sub><span>= (</span><span>1101)</span><sub><span>2</span></sub>，其中<span>1</span>的个数为<span>3</span>，<span>0</span>的个数为<span>1</span>，则称此数为<span>A</span>类数；<p class="MsoNormal"><span>(10)</span><sub><span>10 </span></sub><span>= (</span><span>1010)</span><sub><span>2</span></sub>，其中<span>1</span>的个数为<span>2</span>，<span>0</span>的个数也为<span>2</span>，称此数为<span>B</span>类数；<p class="MsoNormal"><span>(24)</span><sub><span>10 </span></sub><span>= (</span><span>11000)</span><sub><span>2</span></sub>，其中<span>1</span>的个数为<span>2</span>，<span>0</span>的个数为<span>3</span>，则称此数为<span>B</span>类数；<p class="MsoNormal">程序要求：求出<span>1</span>～<span>1000</span>之中（包括<span>1</span>与<span>1000</span>），全部<span>A</span>、<span>B</span>两类数的个数。

 *
 * 输入
 *     无。
 *
 * 输出
 *     一行，包含两个整数，分别是A类数和B类数的个数，中间用单个空格隔开。
 *
 * 样例输入
 *     （无）
 *
 * 样例输出
 *     （不提供）
 *
 * 提示
 *      
 */
 
#include <iostream>
using namespace std;
 
int main(){
    
    return 0;
}